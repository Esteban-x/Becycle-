<div class="container">
    <div class="form-row">
        <div class="col">
            <div class="card">

                <div class="card-body">

                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <button class="btn btn-primary"><i class="far fa-save"></i>&nbsp;Save</button>
                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
</div>





