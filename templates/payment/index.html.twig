{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('assets/css/payment.css') }}" rel="stylesheet">
{% endblock %}
{% block body %}
    <div class="container">
        <form action="{{ path("validate_payment") }}">
            <label for="cardholder-name">Nom</label>
            <input id="cardholder-name" type="text">
            <!-- placeholder for Elements -->
            <div id="card-element"></div>
            <button id="card-button">Submit Payment</button>
        </form>
    </div>

{% endblock %}
