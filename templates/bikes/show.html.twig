{% extends 'base.html.twig' %}

{% block title %}{{bike.name}}{% endblock %}

{% block body %}

<div class="container">
    <div class="row">
        <div class="col-12 col-lg-6">
            <div class="card bg-light mb-3">
                <div class="card-body">
                    <a href="" data-toggle="modal" data-target="#productModal">
                        <img class="img-fluid" src="{{bike.image}}" />
                    </a>
                </div>
            </div>
        </div>

        <div class="col-12 col-lg-6 add_to_cart_block">
                <div class="card bg-light mb-3">
                    <div class="card-body">
                        <h1>{{bike.name}}</h1>
                        <p>{{bike.description}}</p>
                    </div>
                </div>
                        
                       
                <div class="card bg-light mb-3">
                    <div class="card-body">
                        <h1>{{bike.name}}</h1>
                                <p>Caractéristique principale</p>
                            <div class="row">
                            <h2>{{bike.price }}&euro;</h2>
                            <a href="{{ path('addProduct', {'product':bike.id}) }}" class="btn btn-success btn-lg text-uppercase">
                                <i class="fa fa-shopping-cart"></i> Ajouter au panier
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}